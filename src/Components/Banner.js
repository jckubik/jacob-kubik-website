import '../CSS/Banner.css';

const Banner = () => {

  return (
    <section id="banner">
      <div class="inner">
        <header>
          <h2 id="mainName" class="name">Jacob Kubik</h2>
          <h3>I like to build websites - usually with JavaScript and React.</h3>
        </header>
      </div>
    </section>
  );
};

export default Banner;